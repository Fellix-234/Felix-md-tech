# Example environment variables for Felix sessions
# Copy this file to .env and update values as needed.

# Path where session files are stored (relative to project root)
SESSION_PATH=./session

# Default session filename (without extension)
SESSION_NAME=felix

# Server settings
PORT=3000

# Optional: encryption/passphrase for session storage
SESSION_SECRET=change_me
