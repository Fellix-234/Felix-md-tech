import { BOT_INFO } from "./config.js"

export default async function aboutCommand(sock, msg) {
  let text = `🤖 *${BOT_INFO.name}*
  
📌 ${BOT_INFO.description}
🧠 Version: ${BOT_INFO.version}

👑 Developers:
`

  BOT_INFO.developers.forEach(dev => {
    text += `\n• ${dev.name} (${dev.role})\n  ${dev.contact}\n`
  })

  await sock.sendMessage(msg.key.remoteJid, { text })
}
